plugins {
    id 'java-library'
}

description = 'Runtime helpers embedded into protected applications.'

dependencies {
    testImplementation "org.junit.jupiter:junit-jupiter:${rootProject.ext.junitVersion}"
}
